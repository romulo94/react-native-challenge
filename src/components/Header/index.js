import React from "react";

import { View, Text } from "react-native";

import { Container, TextHeader } from "./styles";

const Header = () => (
  <Container>
    <TextHeader>CITIES</TextHeader>
  </Container>
);

export default Header;
